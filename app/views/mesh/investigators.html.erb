<h1>All Investigators tagged with terms matching both <i><%= params[:id] %></i> </h1>
<h3>Terms (<%= @tags.length %>)</h3>
<p><%= @tags.collect{|tag| tag.name+" ("+tag.id.to_s+")"}.join(", ") %></p>

<h3>Investigators  (<%= @investigators.length %>)</h3>
<p>
<%= @investigators.collect{|inv| inv.name+" ("+inv.id.to_s+")"}.join(", ") %>
</p>