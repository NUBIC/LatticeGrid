<% tag_cloud tags, %w(side1 side2 side3 side4) do |tag, css_class| %>
   <%= link_to tag.name, tag_abstract_path(tag.name), :class => css_class, :title => "Total of #{tag.count} tags" %> <br/>
<% end %>
