<% if action_name == 'index' && controller_name == 'welcome' %>
  <script type = 'text/javascript'>
    ddsmoothmenu.init({
      mainmenuid: 'publications_menu',  // Menu DIV id
      orientation: 'v',             // Horizontal or vertical menu: Set to "h" or "v"
      classname: 'ddsmoothmenu-v welcome_column',  // class added to menu's outer DIV
      method: 'toggle',
      contentsource: 'markup',      // "markup" or ["container_id", "path_to_menu_file"]
      // customtheme: ["#804000", "#482400"]
    })
    ddsmoothmenu.init({
      mainmenuid: 'departments_menu',  // Menu DIV id
      orientation: 'v',             // Horizontal or vertical menu: Set to "h" or "v"
      classname: 'ddsmoothmenu-v welcome_column',  // class added to menu's outer DIV
      method: 'toggle',
      contentsource: 'markup',      // "markup" or ["container_id", "path_to_menu_file"]
      // customtheme: ["#804000", "#482400"]
    })
    ddsmoothmenu.init({
      mainmenuid: 'centers_menu',  // Menu DIV id
      orientation: 'v',             // Horizontal or vertical menu: Set to "h" or "v"
      classname: 'ddsmoothmenu-v welcome_column',  // class added to menu's outer DIV
      method: 'toggle',
      contentsource: 'markup',      // "markup" or ["container_id", "path_to_menu_file"]
      // customtheme: ["#804000", "#482400"]
    })
  </script>
<% else %>
  <script type = 'text/javascript'>
    ddsmoothmenu.init({
      mainmenuid: 'side_nav_menu',  // Menu DIV id
      orientation: 'v',             // Horizontal or vertical menu: Set to "h" or "v"
      classname: 'ddsmoothmenu-v',  // class added to menu's outer DIV
      contentsource: 'markup',      // "markup" or ["container_id", "path_to_menu_file"]
      // customtheme: ["#804000", "#482400"]
    })
  </script>
<% end %>
